#!/bin/bash -e

brew install fnm

fnm install --lts
fnm default lts-latest

# This script's shell has no fnm environment yet, so load it before npm.
eval "$(fnm env --shell bash)"
fnm use lts-latest

npm install -g yarn pnpm @dotenvx/dotenvx
